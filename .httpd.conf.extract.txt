# Apache httpd.conf extract providing URL routing and other directives

#   Example usage in httpd.conf:
#   
#   # Online atlas
#   Include /path/to/online-atlas/.httpd.conf.extract.txt
#   Use MacroPopulationsPast "/public-url/populationspast" "/path/to/online-atlas/:/path/to/smarty/:/path/to/php/libraries/" "/path/to/populationspast" "/path/to/online-atlas"


<Macro MacroPopulationsPast $applicationBaseUrl $includePath $localRepoPath $baseRepoPath>
	
	# Local additions
	Alias $applicationBaseUrl/images/about/ $localRepoPath/images/about/
	Alias $applicationBaseUrl/images/logos/ $localRepoPath/images/logos/
	Alias $applicationBaseUrl/images/preview.png $localRepoPath/images/preview.png
	Alias $applicationBaseUrl/resources/ $localRepoPath/resources/
	
	# Load base codebase
	Include $baseRepoPath/.httpd.conf.extract.txt
	Use MacroOnlineAtlas "$applicationBaseUrl" "$baseRepoPath" "Populations Past" "$includePath"
	
</Macro>
